# RoboMind-S3: ESP32-S3 AI Chatbot
# ESP-IDF project root CMakeLists.txt
cmake_minimum_required(VERSION 3.16)

# ESP-IDF projects use this standard pattern:
# The top-level CMakeLists.txt must include $ENV{IDF_PATH}/tools/cmake/project.cmake
# and call project() AFTER the include.

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(robo_mind_s3)
